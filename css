.container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
}

.image {
    flex: 1; /* Menjadikan gambar di sebelah kiri */
}

.text {
    flex: 1; /* Menjadikan teks dan tombol di sebelah kanan */
    text-align: left;
}

button {
    display: block;
    margin-top: 10px;
    padding: 10px 20px;
    background-color: #58a;
    color: white;
    border: none;
    cursor: pointer;
}
